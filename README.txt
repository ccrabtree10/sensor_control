To run the application:

 - Add all of the JARs in the "lib" folder to the Java classpath.
 - Compile all of the Java source files in the "src" folder (and the "test" folder if you want to run the unit tests).
 - Run MainFrame